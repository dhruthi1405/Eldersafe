# Datasets

This folder stores local dataset assets used for training and evaluation.

## Layout

- `raw/` - source videos and original datasets kept locally
- `processed/` - derived CSVs, pose data, and cleaned outputs

## Notes

Keep raw data private and use `processed/` for shareable derived outputs when needed.
