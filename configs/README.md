# Configuration

This folder centralizes runtime settings for the project.

## Main Entry Point

- `config.py` - thresholds, model paths, alert rules, and integration settings

## Usage

Import values from `configs.config` instead of hardcoding paths or thresholds in other modules.
